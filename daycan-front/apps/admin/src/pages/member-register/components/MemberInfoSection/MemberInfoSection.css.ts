import { style } from "@vanilla-extract/css";

export const memberInfoSectionContainer = style({
  display: "flex",
  flexDirection: "column",
  width: "488px",
  gap: 16,
  boxSizing: "border-box",
  borderRadius: 8,
  marginBottom: 60,
});
