import { style } from "@vanilla-extract/css";

export const staffPageContainer = style({
  display: "flex",
  flexDirection: "column",
  width: "1084px",
  boxSizing: "border-box",
});

export const staffButton = style({
  width: "184px",
});

export const staffSearchContainer = style({
  display: "flex",
  flexDirection: "row",
  alignItems: "center",
});
