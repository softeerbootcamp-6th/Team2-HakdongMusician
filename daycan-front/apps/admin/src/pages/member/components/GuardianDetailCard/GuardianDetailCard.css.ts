import { style } from "@vanilla-extract/css";

export const guardianDetailCardContainer = style({
  display: "flex",
  flexDirection: "column",
  gap: "8px",
});

export const guardianDetailCardContentContainer = style({
  display: "flex",
  flexDirection: "row",
  gap: "8px",
});

export const guardianDetailCardContentItemContainer = style({
  display: "flex",
  flexDirection: "column",
  gap: "8px",
});
